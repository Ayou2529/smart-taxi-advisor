# คู่มือการติดตั้งและใช้งาน: Smart Taxi Advisor

## 1. สิ่งที่ต้องมีเบื้องต้น (Prerequisites)

ก่อนเริ่มใช้งาน ต้องเตรียม "กุญแจ" (API Key) 2 อย่างนี้ก่อน (ฟรี):

### 🔑 1.1 Line Notify Token (สำหรับส่งข้อความเตือน)

1.  คลิกที่นี่: [notify-bot.line.me/th/](https://notify-bot.line.me/th/)
2.  เข้าสู่ระบบด้วย **Line ของคุณ**
3.  ไปที่เมนูมุมขวาบน -> เลือก **"หน้าของฉัน" (My Page)**
4.  เลื่อนลงล่างสุด กดปุ่ม **"ออก Token" (Generate Token)**
5.  **ตั้งชื่อ:** พิมพ์ว่า `TaxiAlert`
6.  **เลือกแชท:** เลือกส่งหาตัวเอง (1-on-1) หรือกลุ่มไลน์กับแฟน
7.  **สำคัญ:** คัดลอกรหัสยาวๆ เก็บไว้ทันที!

### 🔑 1.2 AviationStack Key (สำหรับดึงข้อมูลเที่ยวบิน)

1.  คลิกที่นี่: [aviationstack.com/product](https://aviationstack.com/product)
2.  เลือก **"Free Plan"** (0$) -> กด SIGN UP
3.  กรอกข้อมูลสมัครสมาชิกให้เรียบร้อย
4.  เมื่อเข้าหน้า Dashboard ได้แล้ว ให้ก็อปปี้ **"Your API Access Key"** มาเก็บไว้

---

## 2. วิธีตั้งค่า (Configuration)

1. เปิดไฟล์ `taxi_advisor.py` ในโฟลเดอร์นี้
2. แก้ไข 2 บรรทัดแรกในส่วนตั้งค่า:

   ```python
   # ใส่ Token ที่ได้จากข้อ 1.1
   LINE_NOTIFY_TOKEN = "วางรหัส_Line_ยาวๆ_ที่นี่"

   # ใส่ Key ที่ได้จากข้อ 1.2
   AVIATION_STACK_API_KEY = "วางรหัส_Aviation_ที่นี่"

   # เปลี่ยนคำว่า True เป็น False เพื่อเริ่มใช้ข้อมูลจริง
   USE_DEMO_DATA = False
   ```

3. กด Save ไฟล์

---

## 3. วิธีใช้งาน (How to Run)

1. เปิดโฟลเดอร์นี้ใน **Terminal** หรือ **Command Prompt**
2. ติดตั้งตัวช่วย (ทำแค่ครั้งแรกพอ):
   ```bash
   pip install -r requirements.txt
   ```
3. รันโปรแกรม:
   ```bash
   python taxi_advisor.py
   ```
4. รอระบบเช็คข้อมูลประมาณ 5 วินาที ถ้ามีเครื่องลงเยอะ Line จะเด้งเตือนทันที!

---

## 4. ถ้าไม่อยากเปิดคอมทิ้งไว้?

แนะนำให้ใช้ **GitHub Actions** (ฟรี) เพื่อตั้งเวลาให้มันรันเองอัตโนมัติทุกวัน
(ถ้าสนใจวิธีนี้ แจ้งแอดมินให้ช่วยตั้งค่าได้เลยครับ)
